apiVersion: v1
kind: Pod
metadata:
  name: mlperf-gpu-onnx-mobilenet
spec:
  volumes:
    - name: task-pv-storage
      hostPath:
        path: "/mnt/mlperf"
  restartPolicy: Never
  containers:
    - name: mlperf-infer-imgclassify-gpu
      image: tzourhs/mlperf-infer-imgclassify-gpu:1.4
      env:
      - name: DATA_DIR
        value: "/tmp/mlperf/data/imagenet2012"
      - name: MODEL_DIR
        value: "/tmp/mlperf/models/mobilenet_onnx"
      volumeMounts:
        - mountPath: "/tmp/mlperf"
          name: task-pv-storage
      workingDir: "/tmp/inference/vision/classification_and_detection/"
      command: ["/bin/bash", "-c", "./run_local.sh onnxruntime mobilenet gpu --accuracy"]
      resources:
        limits:
          nvidia.com/gpu: 1
